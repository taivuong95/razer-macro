import React from 'react';
import PropTypes from 'prop-types';
import './Editor.css';
const Editor = props => {
    return (
        <div className="widget" id="Editor"/>
    );
};

Editor.propTypes = {
    
};

export default Editor;